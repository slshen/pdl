SAY = GREETING + ", " + SUBJECT
SUBJECT = "world"
GREETING = "hello"
SAY_2 = GREETING + " " + GREETING + " " + SUBJECT

if (ENV == "dev") {
	GREETING = "shouting hello"
}

if (ENV == "prod") {
	GREETING = "shh, quiet pleaes"
}

if (BRANCH) {
	GREETING += BRANCH
}
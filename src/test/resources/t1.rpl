//# comment

/* also block comments */

ENV = "dev"

IS_PROD = ENV ~= "^prod"

// possible future extension
template DB_TEMPLATE {
	TYPE = "oracle"
	PORT = 1521
	if (TYPE == "oracle") {
	   URL = "jdbc:oracle:thin:@" + HOST + ":" + PORT + ":" + SID
	}

}


LC_DB_ follows DB_TEMPLATE

LC_DB_HOST = "usdevdb01.tlcinternal.com"

if (ENV == "dev") {
	DB_NUMBER = 1
	DB_HOST = "devdb01"
	DB_SID = "dev" + DB_NUMBER
}